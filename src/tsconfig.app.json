{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "types": [
      // add node as an option
   ]
  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts"
  ]
}
